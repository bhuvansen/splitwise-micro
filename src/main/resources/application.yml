server:
  port: 8080

spring:
  security:
    jwt:
      secret: verify-splitwise
  datasource:
    url: jdbc:mysql://localhost:3306/splitwise
    username: root
    password: Bhuvan18@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
logging:
  level:
    org.hibernate.SQL: DEBUG

